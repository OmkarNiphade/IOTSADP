int tempPin = A0;
void setup() {
 Serial.begin(9600);
}
void loop() {
 int value = analogRead(tempPin);
 float temp = (value * 5.0 / 1024) * 100;
 if (temp > 30) {
 Serial.println("High Temperature!");
 }
 else {
 Serial.println("Normal Temperature");
 }
 delay(500);
}
